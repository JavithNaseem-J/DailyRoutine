// Basic smoke test — verifies the app mounts without crashing.
// Full unit tests added in Phase 4.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
